Capture the two required runtime screenshots here after starting the application.

ui-preview.png:
- Browser view
- At least two notes visible

delete-action.png:
- Browser view after deleting an item
- DevTools Network tab visible
- Successful DELETE /api/notes/:id request visible

Do not use fabricated screenshots as runtime proof.
